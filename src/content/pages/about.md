---
title: About
---

This is the about page. Edit `src/content/pages/about.md` (or use the CMS at `/admin`) to replace this text.

Pages in `src/content/pages/` render at `/<slug>` — for example, this file becomes `/about`. Add a new markdown file to create a new page.
